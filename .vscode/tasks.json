{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "atcoder_test",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "command": "${workspaceFolder}/cptest.sh",
            "args": [
                "${file}",
                "${fileDirname}"
            ]
        }
    ]
}